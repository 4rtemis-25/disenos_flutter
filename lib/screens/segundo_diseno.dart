import 'package:flutter/material.dart';

class SegundoDisenoScreen extends StatelessWidget {
   
  const SegundoDisenoScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('SegundoDisenoScreen'),
      ),
    );
  }
}